{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build_debug",
      "type": "shell",
      "command": "cmake --build ${workspaceFolder}/out --config Debug --target firmware -j 6",
      "problemMatcher": "$gcc"
    }
  ]
}
